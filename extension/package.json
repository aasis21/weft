{
  "name": "@aasis21/weft-extension",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Weft Copilot CLI extension (joinSession) + local test harness. Source in src/, bundled to dist/extension.mjs (esbuild) with @github/copilot-sdk external.",
  "license": "Apache-2.0",
  "scripts": {
    "build": "node esbuild.config.mjs",
    "harness": "node harness/harness.mjs",
    "test": "node --test"
  },
  "dependencies": {
    "@aasis21/weft-shared": "*",
    "@azure/web-pubsub-client": "^1.1.0",
    "@supabase/supabase-js": "^2.39.7",
    "qrcode": "^1.5.4",
    "ws": "^8.18.0"
  },
  "devDependencies": {
    "esbuild": "^0.25.12"
  },
  "bin": {
    "weft": "bin/weft.mjs"
  }
}
